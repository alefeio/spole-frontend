# API Spolê — porta padrão do backend (ver /api/.env.example: PORT=3000).
# Use o host que o *navegador* consegue alcançar: em WSL/rede, `localhost` no browser
# pode não apontar para o mesmo Postgres/API que o seed; prefira o IP/host real (ex.: http://172.x.x.x:3000).
NEXT_PUBLIC_API_URL=http://localhost:3000

# Modo de UI de pagamento (não altera o body do POST — a API aceita só provider mock-provider).
# - asaas (padrão): homologação/produção; PAYMENTS_PROVIDER=asaas + webhook público no servidor.
# - mock: aviso de dev na UI; PAYMENTS_PROVIDER=mock no servidor.
# Homologação H-19: checklist web/docs/03-qa/mvp-operational-checklist.md (seção 5).
# Backend: PAYMENTS_PROVIDER=asaas + webhooks Asaas → /payments/webhook e /reservation-payments/webhook
NEXT_PUBLIC_PAYMENTS_PROVIDER=asaas

# Opcional (dev): se o WebSocket do HMR falhar ao abrir o app por IP (ex. 172.x), tente acessar por
# http://localhost:PORT ou ajuste firewall/proxy para upgrade WebSocket em `/_next/webpack-hmr`.
